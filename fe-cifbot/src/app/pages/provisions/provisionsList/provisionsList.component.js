import template from './provisionsList.html';
import controller from './provisionsList.controller';

/* @ngInject */
const ProvisionsList = {
  template,
  controller,
  controllerAs: 'vm'
};

export default ProvisionsList;